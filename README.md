# User Details Web Application

React (Vite + Tailwind) frontend with Python (Flask + SQLAlchemy) backend.
Stores user details (name, age, sex, phone) in PostgreSQL or MySQL.

## Quick Start

### Backend (Python)
1. Create a virtual env and install deps:
   - Windows PowerShell:
     ```powershell
     python -m venv .venv
     .venv\\Scripts\\Activate.ps1
     pip install -r backend/requirements.txt
     ```
2. Set `DATABASE_URL` in environment (or copy `backend/.env.example` to `.env` and export manually):
   - PostgreSQL: `postgresql+psycopg2://user:pass@localhost:5432/dbname`
   - MySQL: `mysql+pymysql://user:pass@localhost:3306/dbname`
   - If not set, the app uses a local SQLite file `dev.db` for quick testing.
3. Run the server:
   ```powershell
   set DATABASE_URL=postgresql+psycopg2://user:pass@localhost:5432/dbname  # or mysql+pymysql://...
   python backend/app.py
   ```
   Server runs at http://localhost:5000

### Database Notes
- Ensure the DB exists and credentials are correct.
- Tables are auto-created via `db.create_all()` on startup.

### Frontend (React + Vite + Tailwind)
1. Install Node dependencies:
   ```powershell
   cd frontend
   npm install
   ```
2. Start dev server:
   ```powershell
   npm run dev
   ```
   App runs at http://localhost:5173 and proxies `/api` to http://localhost:5000.

## API
- `GET /api/users` → List users
- `POST /api/users` → Create user
  - Body: `{ name: string, age?: number, sex: 'Male'|'Female'|'Other', phone: string }`

## Customize
- Frontend styles in `frontend/src/styles.css` and Tailwind config.
- Backend models in `backend/models.py`.


```

CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    age INT,
    sex ENUM('male', 'female', 'other') NOT NULL,
    phone VARCHAR(15) UNIQUE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

```


# ===================

# 📊 Prometheus Setup & Overview

## 🔗 Reference Links

- Prometheus Setup Guide:  
  https://vaibhavji.medium.com/deploying-prometheus-and-grafana-for-observability-on-a-minikube-cluster-using-daemonset-266e2df7e454

- Prometheus Hands-on Guide:  
  https://medium.com/@muppedaanvesh/a-hands-on-guide-to-kubernetes-monitoring-using-prometheus-grafana-%EF%B8%8F-b0e00b1ae039

---

# 🚀 What is Prometheus?

Prometheus is an open-source monitoring and alerting toolkit designed for reliability and scalability, especially in cloud-native environments like Kubernetes.

---

# 🧠 Prometheus Server

The **Prometheus Server** is the core component responsible for:

### 🔹 Data Scraping
- Periodically collects metrics from configured targets
- Targets expose metrics via HTTP endpoints

### 🔹 Data Storage
- Stores metrics in a **time-series database**
- Optimized for efficient storage and retrieval

### 🔹 Querying
- Uses **PromQL (Prometheus Query Language)**
- Supports aggregation, filtering, and calculations

---

# ⚙️ Prometheus Components

## 1. Prometheus Server
- Scrapes metrics
- Stores data
- Provides query interface

## 2. Pushgateway
- Used for **short-lived jobs**
- Allows pushing metrics instead of scraping
- Useful for batch jobs

## 3. Exporters

Exporters act as **translators/adapters** between third-party systems and Prometheus.

### ❗ Problem
Prometheus can only collect metrics from systems that expose data in its required format.

### ✅ Solution
Exporters convert metrics into Prometheus-compatible format.

### 🔄 How Exporters Work

1. Connect to third-party system (e.g., database, OS, web server)
2. Collect internal metrics
3. Convert metrics into Prometheus format
4. Expose metrics via an HTTP endpoint (e.g., `/metrics`)
5. Prometheus scrapes the endpoint

---

## 🖥️ Node Exporter Example

- **Third-party system:** Linux/Windows OS  
- **What it does:**
  - Reads CPU usage
  - Memory usage
  - Disk space
  - Network traffic  

- Uses system files (e.g., `/proc` in Linux)

---

## 📦 Common Exporters

| System        | Exporter            | Metrics Exposed |
|--------------|--------------------|----------------|
| PostgreSQL   | Postgres Exporter  | Queries, connections, locks |
| MySQL        | MySQL Exporter     | Buffer pool, query time |
| Nginx        | Nginx Exporter     | Request rate, connections |
| Redis        | Redis Exporter     | Memory usage, key stats |
| AWS CloudWatch | CloudWatch Exporter | EC2, RDS, S3 metrics |

---

# 🚨 Alertmanager

- Handles alerts generated by Prometheus
- Features:
  - Deduplication
  - Grouping
  - Routing

- Sends alerts to:
  - Email
  - Slack
  - PagerDuty
  - Other notification systems

---

# 🔍 Service Discovery

Prometheus supports automatic discovery of targets through:

- Static configuration
- DNS-based discovery
- Kubernetes integration
- Cloud provider integrations

---

# 📐 PromQL (Prometheus Query Language)

- Used to query time-series data
- Supports:
  - Arithmetic operations
  - Aggregations
  - Filtering

---

# 🔄 Data Flow in Prometheus

## 1. Scraping Metrics
Prometheus pulls metrics from targets at regular intervals.

## 2. Storing Metrics
- Stored as time-series data
- Identified by:
  - Metric name
  - Labels (key-value pairs)

## 3. Querying Metrics
- Using PromQL
- Accessed via:
  - Prometheus UI
  - HTTP API
  - Grafana dashboards

## 4. Alerting
- Rules evaluate metrics
- Alerts triggered when conditions are met

## 5. Alert Handling
- Alerts sent to Alertmanager
- Processed and routed to receivers

---

# 🔁 Prometheus Workflow Example

1. **Service Discovery**
   - Discovers pods, services, and nodes (in Kubernetes)

2. **Scraping**
   - Collects metrics from endpoints

3. **Storing**
   - Saves metrics in time-series database

4. **Querying**
   - Analyze data using PromQL

5. **Alerting**
   - Trigger alerts based on defined rules

6. **Alertmanager**
   - Deduplicates, groups, and sends notifications

---

# 🧾 Summary

- Prometheus uses a **pull-based monitoring model**
- Exporters act as **bridges between systems and Prometheus**
- PromQL enables powerful querying and analysis
- Alertmanager ensures efficient alert handling
- Ideal for **Kubernetes and cloud-native observability**

---


# 📊 Prometheus Setup & Overview

## 🔗 Reference Links

- Prometheus Setup Guide:  
  https://vaibhavji.medium.com/deploying-prometheus-and-grafana-for-observability-on-a-minikube-cluster-using-daemonset-266e2df7e454

- Prometheus Hands-on Guide:  
  https://medium.com/@muppedaanvesh/a-hands-on-guide-to-kubernetes-monitoring-using-prometheus-grafana-%EF%B8%8F-b0e00b1ae039

---

# 🚀 What is Prometheus?

Prometheus is an open-source monitoring and alerting toolkit designed for reliability and scalability, especially in cloud-native environments like Kubernetes.

---

# 🧠 Prometheus Server

The **Prometheus Server** is the core component responsible for:

### 🔹 Data Scraping
- Periodically collects metrics from configured targets
- Targets expose metrics via HTTP endpoints

### 🔹 Data Storage
- Stores metrics in a **time-series database**
- Optimized for efficient storage and retrieval

### 🔹 Querying
- Uses **PromQL (Prometheus Query Language)**
- Supports aggregation, filtering, and calculations

---

# ⚙️ Prometheus Components

## 1. Prometheus Server
- Scrapes metrics
- Stores data
- Provides query interface

## 2. Pushgateway
- Used for **short-lived jobs**
- Allows pushing metrics instead of scraping
- Useful for batch jobs

## 3. Exporters

Exporters act as **translators/adapters** between third-party systems and Prometheus.

### ❗ Problem
Prometheus can only collect metrics from systems that expose data in its required format.

### ✅ Solution
Exporters convert metrics into Prometheus-compatible format.

### 🔄 How Exporters Work

1. Connect to third-party system (e.g., database, OS, web server)
2. Collect internal metrics
3. Convert metrics into Prometheus format
4. Expose metrics via an HTTP endpoint (e.g., `/metrics`)
5. Prometheus scrapes the endpoint

---

## 🖥️ Node Exporter Example

- **Third-party system:** Linux/Windows OS  
- **What it does:**
  - Reads CPU usage
  - Memory usage
  - Disk space
  - Network traffic  

- Uses system files (e.g., `/proc` in Linux)

---

## 📦 Common Exporters

| System        | Exporter            | Metrics Exposed |
|--------------|--------------------|----------------|
| PostgreSQL   | Postgres Exporter  | Queries, connections, locks |
| MySQL        | MySQL Exporter     | Buffer pool, query time |
| Nginx        | Nginx Exporter     | Request rate, connections |
| Redis        | Redis Exporter     | Memory usage, key stats |
| AWS CloudWatch | CloudWatch Exporter | EC2, RDS, S3 metrics |

---

# 🚨 Alertmanager

- Handles alerts generated by Prometheus
- Features:
  - Deduplication
  - Grouping
  - Routing

- Sends alerts to:
  - Email
  - Slack
  - PagerDuty
  - Other notification systems

---

# 🔍 Service Discovery

Prometheus supports automatic discovery of targets through:

- Static configuration
- DNS-based discovery
- Kubernetes integration
- Cloud provider integrations

---

# 📐 PromQL (Prometheus Query Language)

- Used to query time-series data
- Supports:
  - Arithmetic operations
  - Aggregations
  - Filtering

---

# 🔄 Data Flow in Prometheus

## 1. Scraping Metrics
Prometheus pulls metrics from targets at regular intervals.

## 2. Storing Metrics
- Stored as time-series data
- Identified by:
  - Metric name
  - Labels (key-value pairs)

## 3. Querying Metrics
- Using PromQL
- Accessed via:
  - Prometheus UI
  - HTTP API
  - Grafana dashboards

## 4. Alerting
- Rules evaluate metrics
- Alerts triggered when conditions are met

## 5. Alert Handling
- Alerts sent to Alertmanager
- Processed and routed to receivers

---

# 🔁 Prometheus Workflow Example

1. **Service Discovery**
   - Discovers pods, services, and nodes (in Kubernetes)

2. **Scraping**
   - Collects metrics from endpoints

3. **Storing**
   - Saves metrics in time-series database

4. **Querying**
   - Analyze data using PromQL

5. **Alerting**
   - Trigger alerts based on defined rules

6. **Alertmanager**
   - Deduplicates, groups, and sends notifications

---

# 🧾 Summary

- Prometheus uses a **pull-based monitoring model**
- Exporters act as **bridges between systems and Prometheus**
- PromQL enables powerful querying and analysis
- Alertmanager ensures efficient alert handling
- Ideal for **Kubernetes and cloud-native observability**

---






